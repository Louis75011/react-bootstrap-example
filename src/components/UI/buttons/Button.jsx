// const button = (props) => {
//     const btnCss = `btn ${props.typeBtn} ${props.width}`

//     return (
//     <button className={btnCss} onClick={props.click}>{props.children}</button>
//     )
// }

// export default button
